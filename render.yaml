services:
  - type: web
    name: asteroids-signaling
    env: node
    plan: free
    buildCommand: cd multiplayer && npm install
    startCommand: node multiplayer/server.js
    envVars:
      - key: PORT
        value: 8080
